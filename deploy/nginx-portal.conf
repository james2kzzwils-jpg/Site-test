# Nginx vhost for the client portal.
# Path: /etc/nginx/sites-available/portal.aepovcg.online
# Symlink: /etc/nginx/sites-enabled/portal.aepovcg.online
#
# Same SSL location as the portfolio host — the cert must cover both
# names (either as a SAN cert listing both, or a wildcard *.aepovcg.online
# + the root cert).
#
# nginx -t && systemctl reload nginx after editing.

server {
    listen 80;
    listen [::]:80;
    server_name portal.aepovcg.online;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name portal.aepovcg.online;

    ssl_certificate     /etc/ssl/aepovcg/fullchain.pem;
    ssl_certificate_key /etc/ssl/aepovcg/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    access_log /var/log/nginx/portal.access.log;
    error_log  /var/log/nginx/portal.error.log;

    # Visiting the root of the portal host should land on /portal
    # directly; everything else passes through to Next.js.
    location = / {
        return 302 /portal;
    }

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_read_timeout 60s;
    }
}
